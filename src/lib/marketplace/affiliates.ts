import type { AffiliateLink } from './types'

export async function getAffiliateLinks(expressionId: number): Promise<AffiliateLink[]> {
  // Placeholder: will integrate with affiliate partner APIs
  // Master of Malt, The Whisky Exchange, Drizly, ReserveBar, Total Wine
  return []
}
